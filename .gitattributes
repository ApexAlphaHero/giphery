# Normalize line endings: LF in the repo, native on checkout.
* text=auto eol=lf

# Windows/Gradle scripts keep their native endings.
*.bat text eol=crlf
gradlew text eol=lf
*.cmd text eol=crlf

# Binary assets — never normalize.
*.gif binary
*.png binary
*.jpg binary
*.keystore binary
*.jar binary
